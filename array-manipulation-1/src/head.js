/* exported head */
function head(array) {
  return array[0];
}

// we want our code to return the first item in the array
// to access an array, we use bracket notation
// we would use bracket notation [0] to access the first element within the array
