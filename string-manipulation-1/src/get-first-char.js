/* exported getFirstChar */
function getFirstChar(string) {
  var firstChar = string[0];
  return firstChar;
}

// we want the function to get the first char from the string passed as an argument.
// we can access the string with bracket notation and assign the returned value to a variable
// we can then return the variable, which will return the first character of the string
