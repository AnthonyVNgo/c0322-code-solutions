/* exported isEmpty */

function isEmpty(stack) {
  // if (stack.peek() === undefined) {
  //   return true;
  // }
  // return false;

  // return stack.peek() === undefined ? true : false
  // why does line 11 pass?
  return stack.peek() === undefined;
}
